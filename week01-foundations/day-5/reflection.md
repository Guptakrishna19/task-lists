# Day 5 Reflection

- **Difficult:** The confusing part was handling API errors properly, especially understanding why a request can fail because of a bad API key, timeout, or connection issue.
- **Improved:** I got better at reading nested JSON, normalizing only the useful fields, loading values from a `.env` file, and saving the final result as JSON.
- **Remains:** I still need more practice with real API keys and checking API documentation so I know which fields are available in each response.
