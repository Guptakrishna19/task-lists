# Day 6 Reflection

- I practiced using `argparse` to accept command-line inputs with `--input` and `--report`.
- I improved at reading CSV files with `csv.DictReader` and converting string values into useful Python types.
- The tricky part was handling bad rows without stopping the whole program, so I collected errors and listed them in the report.
- I also practiced grouping data by month and calculating income, expenses, and balance.
- Remaining improvement: add more exercise files for each smaller skill before the final task.
